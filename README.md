# TDS project

## Setup

Update the value of the `CURRENCY_BEACON_API_KEY` variable in the `.env` file with your key, then run the following command:

```bash
  bun install                                                                                                                                                                                                                                                     
  bun run dev 
```
